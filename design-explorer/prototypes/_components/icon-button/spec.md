# icon-button

Status: **Shared Prototype Component — Draft / Provisional** (not an approved Design System component). First used by: Batch 1 (back, close).

- 44 × 44 hit area around a 24px glyph. `--flush-start` / `--flush-end` pull the hit area outward so the glyph, not the hit area, lines up with the gutter.
- Always needs an accessible name (`aria-label`).
- **Glyphs are TEMPORARY PLACEHOLDERS / NOT SOURCE ASSETS** (`data-asset="temporary-placeholder"`).
- Other glyph uses in the Reference (locate, edit, delete, add, settings) are added at first use.
